# Backend Environment Variables
# These are for reference only. .NET uses appsettings.json for configuration.
# Environment variables override appsettings.json values when set.

# Environment (Development, Staging, Production)
ASPNETCORE_ENVIRONMENT=Development

# Allowed CORS origins (comma-separated)
# For Render, set this to your Vercel frontend URL(s)
AllowedOrigins=http://localhost:3000,http://127.0.0.1:3000

# Port (automatically set by Render, don't set manually for local dev)
# PORT=5000
